argo-cd:
  dex:
    enabled: false
  notifications:
    enabled: false
  applicationSet:
    enabled: false
  server:
    extraArgs:
      - --insecure
    service:
      type: ClusterIP
    # ingress:
    #   enabled: true
    #   ingressClassName: "nginx"
    #   annotations:
    #     "nginx.ingress.kubernetes.io/force-ssl-redirect": "true"
    #     "nginx.ingress.kubernetes.io/ssl-passthrough": "true"
    #     "nginx.ingress.kubernetes.io/backend-protocol": "HTTPS"
    #   tls:
    #     - secretName: "argo-server-tls"
    #       hosts:
    #         - argo.westdynamics.local
  # global:
  #   domain: argo.westdynamics.local
